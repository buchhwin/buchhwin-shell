#%PAM-1.0
# buchhwin-shell lock screen, password only. lock.qml switches to this service
# when the password is submitted while pam_fprintd (system-auth) still waits
# for a finger. Fedora's password-auth has no pam_fprintd.
auth       include      password-auth
account    required     pam_permit.so
